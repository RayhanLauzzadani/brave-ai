<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes - APIs, conventions, and file structure may all differ from your training data. The Next.js app now lives in `frontend/`. Read the relevant guide in `frontend/node_modules/next/dist/docs/` before writing any frontend code. If dependencies have not been reinstalled under `frontend/` yet, the old root `node_modules/next/dist/docs/` may still exist temporarily. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->